{
  "welcomeMessage": "Bienvenido al Director de Juegos de Mesa!",
  "errorMessages": {
    "default": "${$displayName} es inválido.",
    "required": "${$displayName} es necesario.",
    "matches": "${$displayName} tiene el formato incorecto.",
    "email": "${$displayName} no es un email válido.",
    "minLength": "${$displayName} debe tener al menos ${$config.length} carácter${$config.length === 1 ? '' : 'es'}.",
    "maxLength": "${$displayName} no puede tener más de ${$config.length} carácter${$config.length === 1 ? '' : 'es'}.",
    "minItems": "${$displayName} debe contener al menos ${$config.count} ítem${$config.count === 1 ? '' : 'es'}.",
    "maxItems": "${$displayName} no puede contener más de ${$config.count} ítem${$config.count === 1 ? '' : 'es'}.",
    "equals": "${$displayName} debe ser ${$config.expectedValue}."
  },
  "List": "Lista" 
}
